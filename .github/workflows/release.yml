name: Molecular Plus Release

on:
  push:
    tags:
      - "v*.*.*" # Trigger release on version tags

jobs:
  build_windows:
    runs-on: windows-latest
    outputs:
      VERSION: ${{ steps.build.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.13.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.13.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest cython wheel
      - name: Lint with flake8
        run: |
          flake8 . --count --exit-zero --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      - name: Build with Cython
        id: build
        run: |
          $version = python pack_molecular.py
          echo "VERSION=${version}" >> $env:GITHUB_ENV
          echo "VERSION=${version}" >> $env:GITHUB_OUTPUT
      - name: Upload windows zip
        uses: actions/upload-artifact@v4
        with:
          name: molecular-plus_${{ env.VERSION }}_313_win
          path: ./molecular-plus_${{ env.VERSION }}_313_win.zip

  build_linux:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.build.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.13.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.13.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest cython wheel
      - name: Lint with flake8
        run: |
          flake8 . --count --exit-zero --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      - name: Build with Cython
        id: build
        run: |
          version=$(python pack_molecular.py)
          echo "VERSION=${version}" >> $GITHUB_ENV
          echo "VERSION=${version}" >> $GITHUB_OUTPUT
      - name: Upload linux zip
        uses: actions/upload-artifact@v4
        with:
          name: molecular-plus_${{ env.VERSION }}_313_linux
          path: ./molecular-plus_${{ env.VERSION }}_313_linux.zip

  build_macos_arm64:
    runs-on: macos-latest
    outputs:
      VERSION: ${{ steps.build.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.13.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.13.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest cython wheel
          brew install libomp
      - name: Lint with flake8
        run: |
          flake8 . --count --exit-zero --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      - name: Build with Cython
        id: build
        run: |
          version=$(python pack_molecular_arm64.py)
          echo "VERSION=${version}" >> $GITHUB_ENV
          echo "VERSION=${version}" >> $GITHUB_OUTPUT
      - name: Upload mac zip
        uses: actions/upload-artifact@v4
        with:
          name: molecular-plus_${{ env.VERSION }}_313_macos_arm64
          path: ./molecular-plus_${{ env.VERSION }}_313_macos_arm64.zip

  release:
    runs-on: ubuntu-latest
    needs:
      - build_windows
      - build_linux
      - build_macos_arm64
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Debug version
        run: |
          echo "Windows version: ${{ needs.build_windows.outputs.VERSION }}"
          echo "Linux version: ${{ needs.build_linux.outputs.VERSION }}"
          echo "macOS ARM64 version: ${{ needs.build_macos_arm64.outputs.VERSION }}"

      - name: Download artifacts for Windows 313
        uses: actions/download-artifact@v4
        with:
          name: molecular-plus_${{ needs.build_windows.outputs.VERSION }}_313_win
          path: .

      - name: Download artifacts for Linux 313
        uses: actions/download-artifact@v4
        with:
          name: molecular-plus_${{ needs.build_linux.outputs.VERSION }}_313_linux
          path: .

      - name: Download artifacts for macOS 313 arm64
        uses: actions/download-artifact@v4
        with:
          name: molecular-plus_${{ needs.build_macos_arm64.outputs.VERSION }}_313_macos_arm64
          path: .

      - name: Create Release
        run: |
          tag=${GITHUB_REF#refs/tags/}
          gh release create "$tag" \
            molecular-plus_${{ needs.build_windows.outputs.VERSION }}_313_win.zip \
            molecular-plus_${{ needs.build_linux.outputs.VERSION }}_313_linux.zip \
            molecular-plus_${{ needs.build_macos_arm64.outputs.VERSION }}_313_macos_arm64.zip \
            --title "Release $tag" \
            --notes "Release of version $tag"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
